{
  "name": "teachertype-server",
  "version": "1.0.0",
  "description": "Teachertype.ai 백엔드 서버",
  "main": "index.js",
  "scripts": {
    "build": "cd ../client && npm install && npm run build",
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^5.1.0",
    "openai": "^4.9.0"
  }
}